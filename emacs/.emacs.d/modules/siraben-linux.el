;; siraben-linux.el

;; A script to be run when the target OS is gnu/linux.

(setq scheme-program-name "/usr/bin/mit-scheme")
(setq ispell-program-name "/usr/bin/aspell")
(defvar ben/default-font-size 11)

(provide 'siraben-linux)
